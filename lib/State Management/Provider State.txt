

🚀 Provider State Management (Flutter)
১️⃣ Provider আসলে কী?

👉 Provider হলো Flutter-এর state management solution।
তুমি এটাকে ভাবতে পারো →

📦 ডেটা রাখার বাক্স (State) +
📢 সবার সাথে ডেটা শেয়ার করার সিস্টেম (Notifier) +
👂 যেখানে দরকার সেখানে ডেটা শোনার ব্যবস্থা (Consumer)

২️⃣ Provider প্যাটার্নের মূল চরিত্ররা 🎭
🔹 (ক) ChangeNotifier

একটা class → যেখানে state (data) রাখা হয়।

এটি notifyListeners() ব্যবহার করে জানায় → "ডেটা বদলেছে!"

সাধারণত Model / Manager class হিসেবে কাজ করে।

👉 কাজ: State রাখবে এবং update/rebuild signal দেবে।

🔹 (খ) ChangeNotifierProvider

এটি হলো widget wrapper → তোমার ChangeNotifier ক্লাসকে পুরো widget tree তে পৌঁছে দেয়।

Provider tree তে বসালে নিচের সব widget সহজেই সেই state ব্যবহার করতে পারবে।

👉 কাজ: State কে App-এর মধ্যে inject করা।

🔹 (গ) Consumer

Consumer ব্যবহার করে তুমি Provider-এর ডেটা listen করে UI বানাও।

শুধুমাত্র Consumer-এ wrap করা widget-ই rebuild হবে (পুরো widget tree না)।

👉 কাজ: UI-কে provider এর ডেটা শুনতে দেওয়া।

🔹 (ঘ) Provider.of<T>(context)

Provider থেকে ডেটা বের করার শর্টকাট।

দুইভাবে ব্যবহার হয়:

Provider.of<T>(context, listen: true) → listen করবে, UI rebuild হবে।

Provider.of<T>(context, listen: false) → শুধু একবার data নেবে, UI rebuild হবে না।

👉 কাজ: সরাসরি ডেটা নেওয়া।

🔹 (ঙ) context.watch<T>() বনাম context.read<T>()

watch<T>() → ডেটা পরিবর্তন হলে UI rebuild হবে।

read<T>() → ডেটা শুধু পড়বে, কিন্তু listen করবে না।

👉 কোথায় কোনটা?

UI widget → watch ব্যবহার করো।

Button press বা callback → read ব্যবহার করো।

৩️⃣ Provider Flow 🛤️ (ধাপে ধাপে)
১. State ক্লাস তৈরি (ChangeNotifier দিয়ে)
class ThemeProvider extends ChangeNotifier {
  bool _isLightTheme = true;

  bool get isLightTheme => _isLightTheme;

  void toggleTheme() {
    _isLightTheme = !_isLightTheme;
    notifyListeners(); // UI কে জানাচ্ছে → update হও
  }
}

২. State পুরো অ্যাপে ইনজেক্ট করা
void main() {
  runApp(
    ChangeNotifierProvider(
      create: (_) => ThemeProvider(), // এখানে state বসলো
      child: const MyApp(),
    ),
  );
}

৩. UI তে ডেটা ব্যবহার
class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    final themeProvider = context.watch<ThemeProvider>(); // listen করছে

    return MaterialApp(
      theme: themeProvider.isLightTheme ? ThemeData.light() : ThemeData.dark(),
      home: const HomeScreen(),
    );
  }
}

৪. অ্যাকশন নেওয়া (ডেটা পরিবর্তন)
class HomeScreen extends StatelessWidget {
  const HomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Provider Example")),
      body: Center(
        child: Switch(
          value: context.watch<ThemeProvider>().isLightTheme, // ডেটা শোনা
          onChanged: (_) {
            context.read<ThemeProvider>().toggleTheme(); // শুধু অ্যাকশন → read
          },
        ),
      ),
    );
  }
}

৪️⃣ কখন কোন অংশ ব্যবহার করবে? 🧠
অংশ	কী কাজ করে	কখন ব্যবহার করবে
ChangeNotifier	State রাখে, notify করে	ডেটা ও লজিক ম্যানেজ করতে
ChangeNotifierProvider	State কে widget tree তে inject করে	root widget বা যেখানে দরকার সেখানে
Consumer	নির্দিষ্ট widget কে state শোনায়	ছোট widget কে rebuild করতে চাইলে
Provider.of<T>(context)	State access করে	একবার ডেটা নিতে বা listen সহ
context.watch<T>()	listen করে ও rebuild হয়	UI widget এর ভেতরে
context.read<T>()	শুধু ডেটা নেয়, listen না	Button press বা callback এ
৫️⃣ Provider দিয়ে সাধারণত কী কী করা হয়?

🔄 Theme (Light/Dark mode) toggle

🛒 Shopping Cart (item add/remove, total count)

👤 User Login state (logged in/out)

🌐 API Data state (loading/data/error)

✨ সংক্ষিপ্ত ছবি (Visual Flow)

ChangeNotifier (State + Logic)
        ⬇
ChangeNotifierProvider (Inject State in Tree)
        ⬇
Consumer / watch (UI listens to State)
        ⬇
read (Button/Action updates State)
        ⬇
notifyListeners() → Rebuild only listeners


///////////////////////////////////////////////////////////////////////////////////////////////
❓প্রশ্ন ১:

ChangeNotifier-এর notifyListeners() মেথডটির মূল কাজ কী? এটি কল না করলে কী সমস্যা হবে?

✅ উত্তর:

notifyListeners() এর কাজ হলো — provider কে যে সব widget listen করছে তাদের জানানো "state পরিবর্তন হয়েছে → এখন rebuild হও"।

যদি এটা কল না করো →

state (ডেটা) আপডেট হবে ঠিকই,

কিন্তু UI জানবে না state বদলেছে → ফলে UI আপডেট হবে না।

📌 মনে রাখো 👉 Provider = reactive system → notifyListeners() হলো trigger।

❓প্রশ্ন ২:

context.read<T>() এবং context.watch<T>()-এর মধ্যে প্রধান পার্থক্য কী? একটি ElevatedButton-এর onPressed কলব্যাকের ভেতরে কোনটি ব্যবহার করা বেশি যুক্তিযুক্ত এবং কেন?

✅ উত্তর:

context.watch<T>() → Provider থেকে ডেটা নেয় এবং listen করে। মানে notifyListeners() হলে এই widget rebuild হবে।

context.read<T>() → Provider থেকে ডেটা নেয় কিন্তু listen করে না। শুধু একবার access করে।

👉 ElevatedButton-এর onPressed এর ভেতরে context.read<T>() ব্যবহার করা উচিত, কারণ:

এখানে শুধু মেথড কল করতে হবে (যেমন: toggleTheme() বা addToCart())।

rebuild করার দরকার নেই।

তাই watch দিলে অযথা widget rebuild হয়ে পারফরম্যান্স কমতে পারে।

❓প্রশ্ন ৩:

অ্যাপের স্টেট যদি অনেকগুলো স্ক্রিন জুড়ে শেয়ার করার প্রয়োজন হয়, তাহলে ChangeNotifierProvider উইজেটটিকে সাধারণত উইজেট ট্রি-এর কোন জায়গায় রাখা হয়?

✅ উত্তর:

সাধারণত MaterialApp বা CupertinoApp এর উপরে → মানে root এ রাখা হয়।

অনেক সময় main() ফাংশনের ভেতরে runApp() এর child হিসেবে রাখা হয়।

এতে করে অ্যাপের পুরো widget tree থেকে state access করা যায়।

📌 Shortcut মনে রাখো:

local state (কেবল একটি screen এ দরকার) → screen widget এর ভেতরে provider wrap করো।

global state (একাধিক screen এ দরকার) → root এ provider wrap করো।
..............................................................................
📝 কোড ব্যাখ্যা (Line by Line with Comments)
// 🛒 ১. আমাদের মূল State Management Class (ChangeNotifier দিয়ে বানানো হয়েছে)
class ShoppingSystem extends ChangeNotifier {
  int _cartCount = 0;                     // Cart এর item সংখ্যা ধরে রাখবে (private variable)
  int get cartCount => _cartCount;        // Getter → UI থেকে cartCount দেখা যাবে

  int _WishlistCount = 0;                 // Wishlist এর item সংখ্যা ধরে রাখবে
  int get WishlistCount => _WishlistCount; // Getter → UI থেকে wishlistCount দেখা যাবে

  // 🟢 Cart-এ item যোগ করার ফাংশন
  void addToCart(){
    _cartCount++;                         // মান ১ বাড়বে
    notifyListeners();                    // সবাইকে জানাবে → ডেটা বদলেছে, UI রিফ্রেশ করো
  }

  // 🔴 Wishlist-এ item যোগ করার ফাংশন
  void addToWishlist(){
    _WishlistCount++;
    notifyListeners();                    // ডেটা পরিবর্তনের কারণে UI আবার build হবে
  }

  // 🔄 Reset করার ফাংশন
  void resetAll(){
    _cartCount = 0;                       // Cart count = 0
    _WishlistCount = 0;                   // Wishlist count = 0
    notifyListeners();                    // UI-কে জানাবে → update হও
  }
}

void main(){
  runApp(
      ChangeNotifierProvider(             // ২. ChangeNotifierProvider দিয়ে State inject করা হচ্ছে
        create:(_)=> ShoppingSystem(),    // Provider কে বলা হচ্ছে → ShoppingSystem state ব্যবহার করবে
        child: ShoppingCountSystemProvider(), // পুরো অ্যাপ Provider এর child
      )
  );
}

class ShoppingCountSystemProvider extends StatelessWidget {
  const ShoppingCountSystemProvider({super.key});

  @override
  Widget build(BuildContext context) {
    final  cratControl = context.watch<ShoppingSystem>(); // UI listen করবে → data পরিবর্তন হলে rebuild হবে
    final  cratCounterControl = context.read<ShoppingSystem>(); // শুধু ফাংশন কল করার জন্য → listen করবে না

    return Consumer<ShoppingSystem>(      // ৩. Consumer → UI কে জানাবে state পরিবর্তন হলে rebuild হও
      builder: (BuildContext context, ShoppingSystem , child) {
      return MaterialApp(
        debugShowCheckedModeBanner: false,
        home: Scaffold(
            appBar: AppBar(title: const Text('Shopping System'),
              actions: [
                // 🟢 Cart Count দেখানো
                Text("Cart: ${cratControl.cartCount}",
                     style: TextStyle(color: Colors.green,fontWeight: FontWeight.bold),),
                SizedBox(width: 10,),
                // 🔴 Wishlist Count দেখানো
                Text("Wishlist: ${cratControl.WishlistCount}",
                     style: TextStyle(color: Colors.red,fontWeight: FontWeight.bold),),
              ],
            ),

            body: Center(
              child: Card(               // Card UI
                elevation: 5,
                child: Container(
                  padding: EdgeInsets.all(10),
                  width: 300,
                  height: 250,
                  decoration: BoxDecoration(
                    color: Colors.yellow,
                    borderRadius: BorderRadius.circular(10),
                  ),
                  child: Column(
                    mainAxisAlignment: MainAxisAlignment.center,
                    children: [
                      Text("Product Controller",
                          style: TextStyle(fontSize: 20,fontWeight: FontWeight.bold),),
                      SizedBox(height: 10,),
                      Row(
                        mainAxisAlignment: MainAxisAlignment.center,
                        children: [
                          // 🟢 Cart এ যোগ করার বাটন
                          ElevatedButton(
                              onPressed: (){
                                cratCounterControl.addToCart(); // শুধু ফাংশন কল হচ্ছে (read ব্যবহার)
                              },
                              child: Text("Add to Cart")),
                          SizedBox(width: 10,),
                          // 🔴 Wishlist এ যোগ করার বাটন
                          ElevatedButton(
                              onPressed: (){
                                cratCounterControl.addToWishlist();
                              },
                              child: Text("Add to Wishlist")),
                        ],
                      ),
                      SizedBox(height: 10,),
                      // 🔄 সব রিসেট করার বাটন
                      ElevatedButton(
                          onPressed: (){
                            cratCounterControl.resetAll();
                          },
                          child: Text("Reset All")),
                    ],
                  ),
                ),
              ),
            )
        ),
      );
    },
    );
  }
}

📌 এই কোড দিয়ে কী হয়েছে?

State Management করা হয়েছে ShoppingSystem class দিয়ে (Cart ও Wishlist কাউন্ট রাখা, Add, Reset)।

ChangeNotifierProvider → State inject করা হয়েছে পুরো অ্যাপে।

context.watch<ShoppingSystem>() → UI data listen করছে (Cart/Wishlist সংখ্যা দেখানোর জন্য)।

context.read<ShoppingSystem>() → শুধু Action (Button press → add/reset) এ state update করার জন্য ব্যবহার করা হয়েছে।

Consumer → নিশ্চিত করছে state বদলালে UI rebuild হবে।

🎯 চূড়ান্ত প্রভাব (Effect)

🔄 যখন তুমি Add to Cart চাপবে → _cartCount ১ বাড়বে → notifyListeners() হবে → Cart Text আপডেট হবে।

❤️ যখন তুমি Add to Wishlist চাপবে → _WishlistCount ১ বাড়বে → UI তে সংখ্যা বদলাবে।

⭕ যখন তুমি Reset All চাপবে → সব কাউন্ট ০ হয়ে যাবে → UI আবার রিফ্রেশ হবে।

👉 পুরো প্রোজেক্টের মাধ্যমে বোঝানো হয়েছে —
Provider দিয়ে কীভাবে state রাখা, UI আপডেট করা, আর Action অনুযায়ী ডেটা পরিবর্তন করা যায়।....................................