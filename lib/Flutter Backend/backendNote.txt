Asynchronous Programming (async/await):
নেটওয়ার্ক থেকে ডেটা আসতে কিছুটা সময় লাগে। আমরা চাই না এই
সময়টুকু আমাদের অ্যাপটি জমে (freeze) থাকুক। তাই আমরা অ্যাসিঙ্ক্রোনাস প্রোগ্রামিং ব্যবহার করি।

Future: এটি একটি প্রতিশ্রুতি যে ভবিষ্যতে কোনো ডেটা আসবে।
async: কোনো ফাংশনকে async হিসেবে মার্ক করলে, সেটি ব্যাকগ্রাউন্ডে রান করতে পারে।
await: async ফাংশনের ভেতরে কোনো Future-এর কাজ শেষ না হওয়া পর্যন্ত অপেক্ষা করে।

**FutureBuilder দিয়ে ডেটা দেখানো:
FutureBuilder একটি অসাধারণ উইজেট,
যা একটি Future-এর বিভিন্ন অবস্থার (যেমন: লোডিং, ডেটা এসেছে, এরর হয়েছে)
ওপর ভিত্তি করে UI তৈরি করে।
............................................................................
🔍 snapshot কী?

👉 snapshot মানে হলো — Future এর বর্তমান অবস্থা আর ডেটার ছবি (status + data + error একসাথে ধরে রাখে)।
FutureBuilder যখন কাজ করে, তখন snapshot দিয়ে আমরা জানতে পারি:

এখন ডেটা লোড হচ্ছে নাকি শেষ হয়েছে?
ডেটা এসেছে নাকি error হয়েছে?

ডেটা এলে সেটা কী?
🛠️ snapshot এর গুরুত্বপূর্ণ property
1. snapshot.connectionState

Future এর অবস্থা বোঝায়।
প্রধান কয়েকটা state:
waiting → Future এখনো চলছে (লোড হচ্ছে)।
done → Future শেষ হয়ে গেছে (ডেটা এসেছে বা error হয়েছে)।

👉 উদাহরণ:
if (snapshot.connectionState == ConnectionState.waiting) {
  return CircularProgressIndicator(); // Loading দেখাও
}

2. snapshot.hasData
যদি Future থেকে ডেটা আসে → true হবে।
ব্যবহার: ডেটা পেলে সেটি UI তে দেখানো।

👉 উদাহরণ:
if (snapshot.hasData) {
  return Text(snapshot.data.toString()); // ডেটা দেখাও
}

3. snapshot.hasError

যদি Future চলাকালীন কোনো error হয় → true হবে।

ব্যবহার: Error message দেখানো।

👉 উদাহরণ:

if (snapshot.hasError) {
  return Text("Error: ${snapshot.error}");
}

4. snapshot.data

Future থেকে আসা আসল ডেটা এখানে থাকে।
টাইপ: Future যেটা রিটার্ন করে সেটাই হবে।
👉 উদাহরণ:

final users = snapshot.data!; // এখানে ইউজারের লিস্ট আছে

✨ সহজ উদাহরণ দিয়ে মনে রাখো
ধরো তুমি একটা পার্সেল অর্ডার করেছো 📦
connectionState.waiting → ডেলিভারি এখনো পথে (লোডিং)।
hasData → পার্সেল এসে গেছে (ডেটা এসেছে)।
hasError → ডেলিভারি ব্যর্থ হয়েছে (এরর)।
data → পার্সেলের ভেতরে আসল জিনিসপত্র (তোমার আসল ডেটা)।

✅ তাই snapshot হলো Future এর বর্তমান অবস্থা + ফলাফল ধরে রাখার প্যাকেজ।
...........................................................

1. API Endpoint
👉 https://jsonplaceholder.typicode.com/users

এখান থেকে ব্যবহারকারীদের (users) list আনা হবে।

2. Async Function (ডেটা আনা)

👉 fetchUsers() নামের একটি async function বানাবে।
👉 এর ভেতরে:
http.get() দিয়ে API call করবে।
Response কে jsonDecode করবে।
Result হবে List of Users।

3. FutureBuilder (UI তে অবস্থা দেখানো)
👉 FutureBuilder ব্যবহার করলে তিনটি state handle করা যায়:
Loading State → এখনো ডেটা আসছে → CircularProgressIndicator দেখাবে।
Error State → কিছু সমস্যা হলে → Text("Error") দেখাবে।
Data State → সফলভাবে ডেটা এলে → ListView বানাবে।

4. ListView + ListTile (ডেটা দেখানো)

👉 যদি ডেটা আসে, তখন ListView.builder ব্যবহার করবে।
👉 প্রতিটি item হবে ListTile:
title: ব্যবহারকারীর নাম (name)
subtitle: ব্যবহারকারীর ইমেইল (email)
.................................................................
সমস্যা: তুমি FutureBuilder-এর future প্রোপার্টির ভেতরে সরাসরি fetchData() ফাংশনটিকে কল করেছ। এর ফলে, যখনই এই উইজেটটি কোনো কারণে yeniden তৈরি (rebuild)
হবে (যেমন: স্ক্রিন রোটেট করলে বা অন্য কোনো State পরিবর্তন হলে), fetchData() ফাংশনটি আবার কল হবে এবং অপ্রয়োজনীয়ভাবে API রিকোয়েস্ট পাঠাবে।

সমাধান: API কলটি শুধু একবার করার জন্য আমরা initState() মেথড ব্যবহার করব। initState() একটি StatefulWidget-এর জীবনে মাত্র একবারই কল হয়।
//////////////////////////////////////////////////////////////////////////////////////

প্রশ্ন ১: http.get() ফাংশনটি কী ধরনের অবজেক্ট রিটার্ন করে এবং কেন এই প্রক্রিয়াটিকে asynchronous বলা হয়?

http.get() ফাংশনটি Future<Response> অবজেক্ট রিটার্ন করে।

মানে, এটি সরাসরি Response দেয় না, বরং একটি Future দেয়, যেটি ভবিষ্যতে (API থেকে ডেটা আসার পর) Response ডেলিভার করবে।

Asynchronous বলা হয় কারণ এই কাজটি সময়সাপেক্ষ — API সার্ভারে রিকোয়েস্ট পাঠানো, উত্তর আসা — এর মধ্যে অ্যাপকে আটকে রাখা হয় না।
👉 এর ফলে অ্যাপ non-blocking থাকে এবং অন্য কাজ চালিয়ে যেতে পারে।

❓ প্রশ্ন ২: API request সফল হয়েছে কিনা, তা বোঝার জন্য response-এর কোন প্রোপার্টিটি চেক করা হয় এবং সফলতার জন্য তার মান সাধারণত কত থাকে?

API response সফল হলো কিনা তা চেক করতে হয় response.statusCode দিয়ে।

সাধারণত 200 মানে সফল (Success / OK)।

তবে আরও কিছু কোড আছে যেমন:

201 → Created

400 → Bad Request

401 → Unauthorized

404 → Not Found

500 → Server Error

👉 কিন্তু সবচেয়ে বেশি ব্যবহার হয় 200 চেক করার জন্য।

❓ প্রশ্ন ৩: FutureBuilder উইজেটের snapshot অবজেক্টটি কী? এর connectionState এবং hasData প্রোপার্টি দুটির কাজ কী?

FutureBuilder যখন ব্যবহার করি, তখন সে একটি AsyncSnapshot অবজেক্ট (snapshot) পায়।

এই snapshot API কল বা Future-এর বর্তমান অবস্থা জানায়।

🔹 snapshot.connectionState

এটি বোঝায় Future বর্তমানে কোন অবস্থায় আছে:

ConnectionState.waiting → Future এখনো চলছে (লোডিং অবস্থায়)।

ConnectionState.done → Future শেষ হয়েছে (ডেটা এসেছে বা এরর হয়েছে)।

ConnectionState.none → এখনো কোনো Future শুরুই হয়নি।

ConnectionState.active → মাঝে মাঝে স্ট্রিম/একটিভ Future-এর জন্য ব্যবহার হয়।

🔹 snapshot.hasData

যদি Future সফলভাবে ডেটা নিয়ে আসে, তাহলে hasData = true হবে।

এর মানে, snapshot.data থেকে আমরা সেই ডেটা ব্যবহার করতে পারি।

যদি ডেটা না থাকে (বা null হয়), তাহলে hasData = false হবে।

✅ সংক্ষেপে:

http.get() → Future<Response> রিটার্ন করে, async হয় কারণ ডিলে হয়।

Success check → response.statusCode == 200।

FutureBuilder → snapshot = AsyncSnapshot, যা Future-এর অবস্থা ধরে রাখে।

connectionState → Future চলছে নাকি শেষ।

hasData → ডেটা এসেছে কিনা।
