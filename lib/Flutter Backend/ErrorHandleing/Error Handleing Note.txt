❓প্রশ্ন ১: try-catch ব্লক ব্যবহার করার মূল উদ্দেশ্য কী?

উত্তর:

try-catch ব্লক ব্যবহারের উদ্দেশ্য হলো প্রোগ্রাম ক্র্যাশ না করিয়ে Error হ্যান্ডেল করা।

try ব্লকে ঝুঁকিপূর্ণ (যেখানে error আসতে পারে) কোড রাখা হয়, আর যদি error হয় তবে সেটা catch ব্লকে ধরা হয়।

on SocketException ব্যবহার করা হয় ইন্টারনেট সংযোগ না থাকলে (No Internet Error) বা সার্ভারে কানেক্ট করতে না পারলে সেই error ধরার জন্য।

👉 উদাহরণ:

WiFi বন্ধ করলে বা Mobile Data অফ থাকলে API call করলে SocketException ধরা যাবে।

❓প্রশ্ন ২: API থেকে ডেটা লোড হওয়ার সময় ব্যবহারকারীকে একটি ভালো অভিজ্ঞতা দেওয়ার জন্য UI-তে কী দেখানো উচিত এবং কেন?

উত্তর:

Loading State এ: একটি CircularProgressIndicator (লোডিং স্পিনার) বা Shimmer effect দেখানো উচিত।

কেন?

যাতে ব্যবহারকারী বুঝতে পারে যে অ্যাপটি কাজ করছে এবং ডেটা লোড হচ্ছে।

যদি কিছুই না দেখানো হয়, তবে ব্যবহারকারী ভাববে অ্যাপ হ্যাং করেছে বা ডেটা আসছে না।

❓প্রশ্ন ৩: Pagination কী এবং কখন এটি ব্যবহার করা হয়?

উত্তর:

Pagination হলো বড় ডেটাকে ছোট ছোট অংশে ভাগ করে লোড করা।

উদাহরণ: Facebook news feed, YouTube video list → সব ডেটা একসাথে লোড করা হয় না, বরং স্ক্রল করলে নতুন ডেটা লোড হয়।

কেন ব্যবহার করা হয়?

একসাথে অনেক ডেটা লোড করলে অ্যাপ স্লো হয়ে যাবে।

ইউজারের ডেটা খরচ বাঁচে।

সার্ভার এবং মোবাইলের মেমোরি লোড কমে যায়।

👉 সাধারণত লিস্ট বা টেবিলে অনেক ডেটা থাকলে Pagination ব্যবহার করা হয়।