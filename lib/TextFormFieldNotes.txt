🔰 TextFormField বানানোর জন্য Tree Structure:
Form
 └── key: _formKey → ফর্ম নিয়ন্ত্রণ করতে
 └── child: Column → ফর্মের ভিতরে এলিমেন্ট রাখতে
      ├── TextFormField
      │    ├── controller → TextEditingController দিয়ে ধরো
      │    ├── decoration → Label, Hint, Border
      │    └── validator → ইনপুট চেক
      ├── TextFormField (পাসওয়ার্ড)
      │    ├── obscureText: true → লুকিয়ে রাখে
      │    ├── controller → passCtrl
      │    ├── validator → পাসওয়ার্ড শর্ত চেক
      └── ElevatedButton → onPressed এ ফর্ম সাবমিট
            └── if (_formKey.currentState!.validate()) → সঠিক কিনা যাচাই
                  └── ScaffoldMessenger.of(context).showSnackBar(...) → মেসেজ


                Form + _formKey = কন্ট্রোল সেন্টার

                TextFormField + controller = ইনপুট ধরার ব্যবস্থা

                validator = ভুল ইনপুট ধরার নিয়ম

                ScaffoldMessenger = ফলাফল দেখানোর মেসেজ

                obscureText = পাসওয়ার্ড নিরাপদে রাখার উপায়

.................................................................................
✅ Form
সহজ ব্যাখ্যা: Form হলো একটি আবেদনপত্রের মতো। যেমন স্কুলে ভর্তির জন্য একটি আবেদনপত্রে নাম, ঠিকানা, ফোন নম্বরের জন্য আলাদা আলাদা ঘর থাকে, তেমনি ফ্লাটারে Form উইজেটটি নাম, ইমেইল, পাসওয়ার্ডের মতো একাধিক TextFormField-কে একটি গ্রুপ হিসেবে ধরে রাখে।

কাজ: এর মূল কাজ হলো এর ভেতরের সমস্ত ইনপুট ফিল্ডকে একসাথে যাচাই (validate) করা এবং সংরক্ষণ (save) করা।

✅ _formKey (GlobalKey)
সহজ ব্যাখ্যা: এটি হলো সেই আবেদনপত্রের একটি ইউনিক সিরিয়াল নম্বর বা আইডি। ধরুন, অনেকগুলো আবেদনপত্রের মধ্যে আপনি নির্দিষ্ট একটিকে খুঁজে বের করতে চান, তখন এই সিরিয়াল নম্বরটি কাজে লাগে।

কাজ: এই _formKey ব্যবহার করে আপনি কোডের যেকোনো জায়গা থেকে ঐ নির্দিষ্ট Form-কে বলতে পারবেন, "তোমার ভেতরের সব ফিল্ড এখন যাচাই করো (_formKey.currentState!.validate())"।

✅ validator
সহজ ব্যাখ্যা: এটি হলো আবেদনপত্রের প্রতিটি ঘরের পাশে লেখা নিয়মাবলী। যেমন: "নামের ঘর খালি রাখা যাবে না" বা "বয়স অবশ্যই ১৮ বছরের বেশি হতে হবে"।

কাজ: validator-এর ভেতরে আপনি একটি নিয়ম লিখে দেন। ব্যবহারকারী যখন কিছু ইনপুট দেয়, এই নিয়মটি চেক করা হয়। যদি নিয়ম ভঙ্গ হয় (যেমন: নামের ঘর খালি রাখা হয়), তাহলে এটি একটি এরর মেসেজ রিটার্ন করে যা স্ক্রিনে দেখানো হয়। আর যদি সব ঠিক থাকে, তাহলে null রিটার্ন করে।

✅ TextEditingController
সহজ ব্যাখ্যা: এটি হলো প্রতিটি ইনপুট বক্সের জন্য একটি রিমোট কন্ট্রোল 🎮। এই রিমোট দিয়ে আপনি জানতে পারেন বক্সে কী লেখা আছে (.text), বক্সের লেখা মুছে ফেলতে পারেন (.clear()), বা নতুন কোনো লেখা সেট করতে পারেন।

কাজ: TextFormField-এর ভেতরের লেখা পড়া বা পরিবর্তন করার জন্য এটি অপরিহার্য।

✅ ScaffoldMessenger.of(context)
সহজ ব্যাখ্যা: এটি হলো আপনার অ্যাপের বর্তমান স্ক্রিনের জন্য একজন বার্তা বাহক বা মেসেঞ্জার messenger। আপনি তাকে একটি বার্তা (SnackBar) দিলে, সে সেটি স্ক্রিনের নিচে সুন্দর করে কিছুক্ষণের জন্য দেখিয়ে আবার চলে যায়।

কাজ: ব্যবহারকারীকে কোনো ছোট তথ্য (যেমন: "Login Successful" বা "No Internet") জানানোর জন্য এটি ব্যবহৃত হয়।

✅ obscureText: true
সহজ ব্যাখ্যা: এটি পাসওয়ার্ড ফিল্ডের জন্য একটি গোপনীয়তার পর্দা। যখন এটি true করা হয়, তখন আপনি যা-ই টাইপ করুন না কেন, স্ক্রিনে শুধু * বা • চিহ্ন দেখা যায়, আসল অক্ষরগুলো দেখা যায় না।

কাজ: পাসওয়ার্ড বা অন্য কোনো সংবেদনশীল তথ্য টাইপ করার সময় পাশের লোক যেন তা দেখে ফেলতে না পারে, তা নিশ্চিত করা।

TextFormField
│
├── 🧠 controller: (নিয়ন্ত্রণের জন্য)
│   └── TextEditingController() // লেখা পড়া বা পরিবর্তনের জন্য রিমোট কন্ট্রোল।
│
├── 🎨 decoration: (সাজানোর জন্য)
│   └── InputDecoration( // ফিল্ডের ভেতরের ও বাইরের স্টাইল।
│       │
│       ├── labelText: "Enter your name"  // ফিল্ডের উপরে ভাসমান একটি লেখা।
│       ├── hintText: "e.g., John Doe"     // ফিল্ডের ভেতরে হালকা করে দেখানো উদাহরণ।
│       ├── prefixIcon: Icon(Icons.person) // ফিল্ডের লেখার আগে একটি আইকন।
│       ├── suffixIcon: Icon(Icons.visibility) // ফিল্ডের লেখার পরে একটি আইকন।
│       └── border: OutlineInputBorder()    // ফিল্ডের চারপাশে একটি বর্ডার।
│
├── ⚖️ validator: (যাচাই করার জন্য)
│   └── (value) { // একটি ফাংশন যা ইনপুট করা 'value' চেক করে।
│       │
│       ├── if (value is empty) { return "Error message"; } // নিয়ম ভঙ্গ হলে এরর মেসেজ।
│       └── return null; // নিয়ম সঠিক হলে null।
│
├── 🔐 obscureText: true // পাসওয়ার্ডের জন্য লেখা গোপন করতে।
│
└── ⌨️ keyboardType: (কিবোর্ডের ধরন)
    └── TextInputType.emailAddress // ইমেইল লেখার জন্য @ সহ কিবোর্ড।
    └── TextInputType.number       // শুধু নম্বর লেখার জন্য কিবোর্ড।

    .,,..................................................................................................
    Importent: Automatic

    👉 Flutter/Dart এর email_validator প্যাকেজও ব্যবহার করা যায়:

    dependencies:
      email_validator: ^2.0.1

    import 'package:email_validator/email_validator.dart';

    validator: (value) {
      if (!EmailValidator.validate(value!)) {
        return 'Invalid email';
      }
      return null;
    }



    ........................................................
Manual:

    validator: (value) {
      if (value == null || value.isEmpty) {
        return 'Email cannot be empty';
      } else if (!RegExp(r'^[\w-\.]+@([\w-]+\.)+[\w-]{2,4}$').hasMatch(value)) {
        return 'Enter a valid email address';
      }
      return null;
    },

🧠 ব্যাখ্যা:
RegExp() → এটি একটি Regular Expression (নিয়ম অনুযায়ী pattern) চেক করে

hasMatch(value) → ইউজার ইনপুট ইমেইল ওই প্যাটার্নে ম্যাচ করে কিনা তা দেখে

| ইনপুট            | বৈধ? | কেন?                 |
| ---------------- | ---- | -------------------- |
| `abc`            | ❌    | @ নেই                |
| `abc@`           | ❌    | ডোমেইন নেই           |
| `abc@gmail`      | ❌    | `.com` বা `.org` নেই |
| `abc @gmail.com` | ❌    | স্পেস আছে            |
| `abc@gmail.com`  | ✅    | বৈধ                  |

!RegExp(r'^[\w-\.]+@([\w-]+\.)+[\w-]{2,4}$').hasMatch(value)

এটার মানে কী ধাপে ধাপে বুঝে নিই:

🔹 Step 1: RegExp(...)
এটি Dart এর Regular Expression ক্লাস, যেটা দিয়ে pattern match করা হয়।

🔹 Step 2: r'^[\w-\.]+@([\w-]+\.)+[\w-]{2,4}$'
এটাই হচ্ছে ইমেইল যাচাই করার pattern।
এটা একটু জটিল দেখালেও আমি তোমার জন্য সহজ করে ভেঙে বুঝিয়ে দিচ্ছি:

✅ RegExp ব্যাখ্যা (ভাঙা অংশে):
| অংশ           | মানে                                                             |
| ------------- | ---------------------------------------------------------------- |
| `^`           | স্ট্রিং শুরু থেকে চেক শুরু                                       |
| `[\w-\.]+`    | ইউজারনেম অংশ: যেকোনো সংখ্যা, লেটার, `_`, `-`, `.` (এক বা একাধিক) |
| `@`           | একদম মাঝখানে `@` থাকতে হবে                                       |
| `([\w-]+\.)+` | ডোমেইনের অংশ: যেমন `gmail.` (এক বা একাধিক হতে পারে)              |
| `[\w-]{2,4}`  | শেষ অংশ: যেমন `com`, `net` ইত্যাদি (২ থেকে ৪ অক্ষর)              |
| `$`           | স্ট্রিংয়ের শেষ পর্যন্ত চেক করে                                   |

উদাহরণ:
✅ ম্যাচ করে:
abc@gmail.com

name.saidur@domain.org

❌ ম্যাচ করে না:
abc → কারণ @ নেই

abc@ → কারণ ডোমেইন নেই

abc@gmail → .com নেই

abc@gmail.toolong → শেষের অংশ ৪ অক্ষরের বেশি

🔹 Step 3: .hasMatch(value)
ইউজারের ইনপুট value এই pattern এর সঙ্গে মিলছে কিনা তা চেক করে।

true হলে → ইমেইল valid ✅

false হলে → invalid ❌
...................................................................................................
----------------------------
Code Sumarize1
----------------------------
import 'package:flutter/cupertino.dart';               // iOS স্টাইলের উইজেট ব্যবহারের জন্য ইম্পোর্ট করা হয়েছে (যদিও কোডে ব্যবহৃত হয়নি)।
import 'package:flutter/material.dart';                 // ম্যাটেরিয়াল ডিজাইনের সমস্ত উইজেট ব্যবহারের জন্য ইম্পোর্ট করা হয়েছে।

// এটি একটি StatelessWidget, কারণ এর অবস্থা (state) সরাসরি এই ক্লাসে পরিবর্তন হয় না।
class TextFormFieldEditingController extends StatelessWidget {
  TextFormFieldEditingController({super.key});      // ক্লাসের কনস্ট্রাক্টর।

  // ফর্মকে শনাক্ত এবং নিয়ন্ত্রণ করার জন্য একটি ইউনিক চাবি বা আইডি তৈরি করা হয়েছে।
  final _formKey = GlobalKey<FormState>();
  // 'নাম' ইনপুট ফিল্ডের লেখা পড়া বা পরিবর্তনের জন্য একটি কন্ট্রোলার।
  TextEditingController nameC = TextEditingController();
  // 'ইমেইল' ইনপুট ফিল্ডের জন্য আরেকটি কন্ট্রোলার।
  TextEditingController emailC = TextEditingController();
  // 'পাসওয়ার্ড' ইনপুট ফিল্ডের জন্য তৃতীয় কন্ট্রোলার।
  TextEditingController passC = TextEditingController();

  // স্ক্রিনের নিচে একটি মেসেজ (SnackBar) দেখানোর জন্য একটি হেল্পার ফাংশন।
  snkbtn(msg, context) {
    ScaffoldMessenger.of(context).showSnackBar(SnackBar(content: Text(msg)));
  }

  @override
  Widget build(BuildContext context) {                  // এই মেথডটি স্ক্রিনের UI তৈরি করে।
    return Scaffold(                                    // অ্যাপের মূল পেজ কাঠামো।
      appBar: AppBar(                                   // পেজের উপরের অংশ বা টাইটেল বার।
        title: Text("TextFormFieldEditingController"),  // AppBar-এর টাইটেল।
        leading: Icon(Icons.text_decrease),             // AppBar-এর বাম পাশে একটি আইকন।
        backgroundColor: Colors.amber,                  // AppBar-এর الخلفية রঙ।
      ),
      body: SafeArea(                                   // UI-কে ফোনের নচ বা সিস্টেম বার থেকে নিরাপদ রাখে।
        child: Padding(                                 // সবদিকে ২০ পিক্সেল ফাঁকা জায়গা তৈরি করে।
          padding: EdgeInsets.all(20),
          child: Form(                                  // একাধিক ইনপুট ফিল্ডকে একটি গ্রুপ হিসেবে ধরে রাখে।
            key: _formKey,                              // ফর্মের সাথে পূর্বে তৈরি করা চাবিটি যুক্ত করা হলো।
            child: Column(                              // ভেতরের উইজেটগুলোকে ওপর থেকে নিচে সাজায়।
              mainAxisAlignment: MainAxisAlignment.start, // কলামের আইটেমগুলো ওপর থেকে শুরু হবে।
              children: [
                TextFormField(                          // 'নাম'-এর জন্য একটি ইনপুট ফিল্ড।
                  controller: nameC,                    // এই ফিল্ডটিকে 'nameC' কন্ট্রোলার দিয়ে নিয়ন্ত্রণ করা হবে।
                  decoration: InputDecoration(          // ফিল্ডটিকে সুন্দরভাবে সাজানোর জন্য।
                      labelText: "Name",                // ফিল্ডের উপরে একটি লেবেল।
                      hintText: "Enter Your Valid Name",// ফিল্ডের ভেতরে একটি উদাহরণ লেখা।
                      border: OutlineInputBorder(),     // ফিল্ডের চারপাশে একটি বর্ডার।
                      suffixIcon: Icon(Icons.person)    // ফিল্ডের শেষে একটি আইকন।
                  ),
                  validator: (value) {                  // এই ফিল্ডের লেখা যাচাই করার নিয়ম।
                    if (value!.isEmpty || value == null) { // যদি লেখাটি খালি বা null হয়।
                      return 'Name cannot be empty';    // তাহলে এই এরর মেসেজটি দেখানো হবে।
                    }
                    return null;                        // যদি সব ঠিক থাকে, তাহলে কোনো এরর নেই।
                  },
                ),
                SizedBox(height: 10),                   // দুটি ফিল্ডের মধ্যে ১০ পিক্সেল ফাঁকা জায়গা।

                TextFormField(                          // 'ইমেইল'-এর জন্য একটি ইনপুট ফিল্ড।
                  keyboardType: TextInputType.emailAddress, // কিবোর্ডটি ইমেইল লেখার উপযোগী হবে।
                  controller: emailC,                   // এই ফিল্ডটিকে 'emailC' কন্ট্রোলার দিয়ে নিয়ন্ত্রণ করা হবে।
                  decoration: InputDecoration(labelText: "Email", hintText: "Ex: example@gmail.com", border: OutlineInputBorder(), suffixIcon: Icon(Icons.mail)),
                  validator: (value) {                  // ইমেইল যাচাই করার নিয়ম।
                    if (value!.isEmpty || value == null) { // যদি ফিল্ডটি খালি থাকে।
                      return 'Email cannot be empty';   // তাহলে এই এরর মেসেজ দেখানো হবে।
                    } else if (RegExp(r'^[\w-\.]+@([\w-]+\.)+[\w-]{2,4}$').hasMatch(value) == false) { // যদি লেখাটি একটি সঠিক ইমেইল ফরম্যাটে না থাকে।
                      return 'Email is not valid';      // তাহলে এই এরর মেসেজ দেখানো হবে।
                    }
                    return null;                        // যদি সব ঠিক থাকে, তাহলে কোনো এরর নেই।
                  },
                ),
                SizedBox(height: 10),                   // দুটি ফিল্ডের মধ্যে ১০ পিক্সেল ফাঁকা জায়গা।

                TextFormField(                          // 'পাসওয়ার্ড'-এর জন্য একটি ইনপুট ফিল্ড।
                  controller: passC,                    // এই ফিল্ডটিকে 'passC' কন্ট্রোলার দিয়ে নিয়ন্ত্রণ করা হবে।
                  decoration: InputDecoration(labelText: "Password", hintText: "Enter Your Valid Password", border: OutlineInputBorder(), suffixIcon: Icon(Icons.lock)),
                  validator: (value) {                  // পাসওয়ার্ড যাচাই করার নিয়ম।
                    if (value!.isEmpty) {               // যদি ফিল্ডটি খালি থাকে।
                      return 'Password cannot be empty';// তাহলে এই এরর মেসেজ দেখানো হবে।
                    } else if (value.length < 6) {      // যদি পাসওয়ার্ডের দৈর্ঘ্য ৬ অক্ষরের কম হয়।
                      return 'Password must be at least 6 characters'; // তাহলে এই এরর মেসেজ দেখানো হবে।
                    }
                    return null;                        // যদি সব ঠিক থাকে, তাহলে কোনো এরর নেই।
                  },
                ),
                SizedBox(height: 10),                   // ফিল্ড এবং বাটনের মধ্যে ১০ পিক্সেল ফাঁকা জায়গা।
                ElevatedButton(                         // একটি বাটন তৈরি করা হয়েছে।
                  onPressed: () {                       // বাটন চাপলে এই কোডটি রান হবে।
                    if (_formKey.currentState!.validate()) { // ফর্মের সব validator চেক করা হচ্ছে।
                      // যদি সব ফিল্ডের ভ্যালিডেশন সঠিক হয়...
                      snkbtn("Account Created for [${nameC.text}]!", context); // তাহলে সফল বার্তা দেখানো হবে।
                    }
                  },
                  child: Text("Submit"),                // বাটনের ভেতরের লেখা।
                  style: ElevatedButton.styleFrom(      // বাটনের স্টাইল পরিবর্তন করার জন্য।
                      backgroundColor: Colors.lightBlueAccent, // বাটনের الخلفية রঙ।
                      foregroundColor: Colors.black      // বাটনের লেখার রঙ।
                  ),
                ),
              ],
            ),
          ),
        ),
      ),
    );
  }
}


